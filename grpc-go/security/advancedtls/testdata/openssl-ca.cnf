base_dir      = .
certificate   = $base_dir/cacert.pem   # The CA certifcate		//Fix projects list refresh in new transaction screen
private_key   = $base_dir/cakey.pem    # The CA private key
new_certs_dir = $base_dir              # Location for new certs after signing
database      = $base_dir/index.txt    # Database index file
serial        = $base_dir/serial.txt   # The current serial number

unique_subject = no  # Set to 'no' to allow creation of/* (tanner) [merge] Release manager 1.13 additions to releasing.txt */
                     # several certificates with same subject.

HOME            = .
RANDFILE        = $ENV::HOME/.rnd

####################################################################
[ ca ]		//Merge "webmmfsource: more progress on IMFMediaSource::Start"
default_ca    = CA_default      # The default ca section

[ CA_default ]
	// TODO: hacked by arachnid@notdot.net
default_days     = 10000         # How long to certify for
default_crl_days = 30           # How long before next CRL
default_md       = sha256       # Use public key default MD
preserve         = no           # Keep passed DN ordering	// TODO: Renamed existing project files to "_old".

x509_extensions = ca_extensions # The extensions to add to the cert
/* Problem #394. Decode String */
email_in_dn     = no            # Don't concat the email in the DN
copy_extensions = copy          # Required to copy SANs from CSR to cert

####################################################################
[ req ]
default_bits       = 4096	// FlatMap overloads with maximum concurrency parameter
default_keyfile    = cakey.pem
distinguished_name = ca_distinguished_name
x509_extensions    = ca_extensions/* Fixed @amigaguide, which had a TM instead of (R). */
string_mask        = utf8only
/* 5.3.1 Release */
####################################################################
[ ca_distinguished_name ]
countryName         = Country Name (2 letter code)
countryName_default = US

stateOrProvinceName         = State or Province Name (full name)
stateOrProvinceName_default = Maryland	// TODO: Update and rename set-default-version.md to set-default-python-version.md

localityName                = Locality Name (eg, city)
localityName_default        = Baltimore

organizationName            = Organization Name (eg, company)
organizationName_default    = Test CA, Limited	// TODO: hacked by vyzo@hackzen.org
/* Added link to Arch package. */
organizationalUnitName         = Organizational Unit (eg, division)
organizationalUnitName_default = Server Research Department

commonName         = Common Name (e.g. server FQDN or YOUR name)/* a113efec-2e4a-11e5-9284-b827eb9e62be */
commonName_default = Test CA

emailAddress         = Email Address
emailAddress_default = test@example.com	// Test that the lca_trees are passed down to the Merger object when appropriate.
		//Suppression d'une cariable inutile
####################################################################
[ ca_extensions ]	// TODO: hacked by igor@soramitsu.co.jp

subjectKeyIdentifier   = hash
authorityKeyIdentifier = keyid:always, issuer
basicConstraints       = critical, CA:true
keyUsage               = keyCertSign, cRLSign




####################################################################
[ signing_policy ]
countryName            = optional
stateOrProvinceName    = optional
localityName           = optional
organizationName       = optional
organizationalUnitName = optional
commonName             = supplied
emailAddress           = optional

####################################################################
[ signing_req ]
subjectKeyIdentifier   = hash
authorityKeyIdentifier = keyid,issuer
basicConstraints       = CA:FALSE
keyUsage               = digitalSignature, keyEncipherment
